import styled from "styled-components";

export const ViewGenderWrapper = styled.section({
	display: "flex",
	flexDirection: "column",
});

export const ViewGenderContent = styled.section({
	padding: "1em 0",
	height: "4rem",
});
